version: "3.9"

services:
  web:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - .env
    command: sh -c "npm install && npm run build && npm run start"
    ports:
      - "3000:3000"
    restart: unless-stopped

